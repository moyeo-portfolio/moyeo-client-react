export interface StackINF {
    status: number,
    lang: String,
    tool: String,
    contents: String,
    status_tool: boolean,
}

export interface ToolINF {
    tool: String,
}

export interface LangINF {
    lang: String,
    contents: String,
}